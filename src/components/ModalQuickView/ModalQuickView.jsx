import React from "react";
import {} from "./index";

// === Styled Components ===
import styled from "styled-components";
export const Wrapper = styled.aside``;
const ModalQuickView = () => {
  return <Wrapper>ModalQuickView</Wrapper>;
};

export default ModalQuickView;
