import React from "react";
import { CloseIconContainer } from "./index";
const CloseIcon = ({ ModalQuickView, click }) => {
  return <CloseIconContainer ModalQuickView={ModalQuickView} onClick={click} />;
};

export default CloseIcon;
